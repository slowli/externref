[package]
name = "externref-test"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
externref = { version = "0.1.0", path = ".." }

[dev-dependencies]
assert_matches = "1.5.0"
wasmtime = "0.37.0"
externref-processor = { version = "0.1.0", path = "../crates/processor" }
